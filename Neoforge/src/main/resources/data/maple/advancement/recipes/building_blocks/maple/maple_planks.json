{
  "parent": "minecraft:recipes/root",
  "criteria": {
    "AdvancementCriterion[trigger=net.minecraft.advancement.criterion.InventoryChangedCriterion@4279aac1, triggerInstance=Conditions[player=Optional.empty, slots=Slots[occupied=IntRange[min=Optional.empty, max=Optional.empty, minSq=Optional.empty, maxSq=Optional.empty], full=IntRange[min=Optional.empty, max=Optional.empty, minSq=Optional.empty, maxSq=Optional.empty], empty=IntRange[min=Optional.empty, max=Optional.empty, minSq=Optional.empty, maxSq=Optional.empty]], items=[ItemPredicate[items=Optional[NamedSet(TagKey[minecraft:item / maple:maple_logs])[[]]], count=IntRange[min=Optional.empty, max=Optional.empty, minSq=Optional.empty, maxSq=Optional.empty], components=[], subPredicates={}]]]]": {
      "conditions": {
        "items": [
          {
            "items": "#maple:maple_logs"
          }
        ]
      },
      "trigger": "minecraft:inventory_changed"
    },
    "has_the_recipe": {
      "conditions": {
        "recipe": "minecraft:maple/maple_planks"
      },
      "trigger": "minecraft:recipe_unlocked"
    }
  },
  "requirements": [
    [
      "has_the_recipe",
      "AdvancementCriterion[trigger=net.minecraft.advancement.criterion.InventoryChangedCriterion@4279aac1, triggerInstance=Conditions[player=Optional.empty, slots=Slots[occupied=IntRange[min=Optional.empty, max=Optional.empty, minSq=Optional.empty, maxSq=Optional.empty], full=IntRange[min=Optional.empty, max=Optional.empty, minSq=Optional.empty, maxSq=Optional.empty], empty=IntRange[min=Optional.empty, max=Optional.empty, minSq=Optional.empty, maxSq=Optional.empty]], items=[ItemPredicate[items=Optional[NamedSet(TagKey[minecraft:item / maple:maple_logs])[[]]], count=IntRange[min=Optional.empty, max=Optional.empty, minSq=Optional.empty, maxSq=Optional.empty], components=[], subPredicates={}]]]]"
    ]
  ],
  "rewards": {
    "recipes": [
      "minecraft:maple/maple_planks"
    ]
  }
}